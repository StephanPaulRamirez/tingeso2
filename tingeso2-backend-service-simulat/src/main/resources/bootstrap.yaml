spring:
  application:
    name: simulateService
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/Simulate
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true